#include <iostream>
using namespace std;


int main() {
	//Write a program to check if a number is prime.

    int n;
    cin >> n;
    bool prime = (n > 1);  // assume prime if > 1

    for (int i = 2; i * i <= n && prime; i++) {
        if (n % i == 0) prime = false;
    }

    cout << (prime ? "Prime" : "Not Prime") << endl;
}
//#include <iostream>
//using namespace std;
//Bruteforcemethod
//int main() {
//    int n;
//    cout << "Enter a number: ";
//    cin >> n;
//
//    bool isPrime = true; // assume the number is prime
//
//    if (n <= 1) {
//        isPrime = false; // 0 and 1 are not prime
//    }
//    else {
//        // check divisibility from 2 to n-1 (brute-force)
//        for (int i = 2; i < n; i++) {
//            if (n % i == 0) { // divisible → not prime
//                isPrime = false;
//                break;         // stop as soon as a divisor is found
//            }
//        }
//    }
//
//    if (isPrime)
//        cout << n << " is a prime number" << endl;
//    else
//        cout << n << " is not a prime number" << endl;
//
//    return 0;
//}

